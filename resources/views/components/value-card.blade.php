<div class="p-8 border border-slate-100 dark:border-slate-800 rounded-2xl hover:border-primary/30 hover:bg-primary/5 transition-all group">
    <div class="size-12 bg-primary/10 rounded-lg flex items-center justify-center text-primary mb-6 group-hover:bg-primary group-hover:text-white transition-colors">
        <span class="material-symbols-outlined">{{ $icon }}</span>
    </div>
    <h4 class="text-xl font-bold mb-2">{{ $title }}</h4>
    <p class="text-slate-500">{{ $description }}</p>
</div>